{
  "name": "skill-harness",
  "version": "0.1.0",
  "description": "Harness Engine for Skill development - Lint, Compile, Debug, Profile",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "npm run build && node dist/lint/cli.js",
    "compile": "npm run build && node dist/compile/cli.js",
    "debug": "npm run build && node dist/debug/cli.js",
    "profile": "npm run build && node dist/profile/cli.js",
    "runtime": "npm run build && node dist/runtime/cli.js",
    "harness": "npm run build && node dist/workflow/cli.js",
    "config": "npm run build && node dist/config/cli.js",
    "dev": "tsc --watch"
  },
  "keywords": ["skill", "lint", "agent", "harness"],
  "license": "MIT",
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/jest": "^29.5.0",
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.0.0",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.0",
    "typescript": "^5.3.0"
  }
}
